
{
  "name": "hermes-obsidian-plugin",
  "version": "1.0.0",
  "description": "Obsidian plugin for Hermes Voice Assistant",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild main.ts --bundle --outfile=main.js --external:obsidian --format=cjs --watch",
    "build": "esbuild main.ts --bundle --outfile=main.js --external:obsidian --format=cjs --minify"
  },
  "dependencies": {
    "@google/genai": "^1.38.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "tslib": "^2.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.0",
    "obsidian": "^1.1.1",
    "typescript": "^4.9.0"
  }
}
